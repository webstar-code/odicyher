/**
 * Default auditor logo when `brandLogoSrc` is empty or missing on an audit report.
 * File must exist under `public/` (see `next/image` restrictions).
 */
export const DEFAULT_BRAND_LOGO_SRC = "/images/logo.png";
